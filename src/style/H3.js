import styled from 'styled-components';
import { systemFont } from 'style/constants';

const H3 = styled.h3`
  font-family: ${systemFont};
  font-size: 24px;
  font-weight: 600;
  opacity: 0.8;
`;

export default H3;